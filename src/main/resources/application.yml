spring:
  application:
    name: api-test-agent
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY:}
      chat:
        options:
          model: claude-haiku-4-5-20251001
  thymeleaf:
    cache: false

server:
  port: 8080

api-test-agent:
  data-dir: ./data
  storage:
    schema-version: "1.0"

logging:
  level:
    root: INFO
    com.dialog.dtg: INFO
